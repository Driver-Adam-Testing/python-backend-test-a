<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

Configuration settings for a local test environment, including domain, backend, email, database, and AWS.

# Purpose
The configuration file defines various environment settings and credentials for deploying and running the `Driver AI App`. It specifies the domain and environment type, such as `local`, `staging`, or `production`, and includes project-specific identifiers like `PROJECT_NAME` and `STACK_NAME`. Backend settings include `BACKEND_CORS_ORIGINS` to manage cross-origin resource sharing and a flag for user registration. Email configurations are set with SMTP details, including host, user, password, and security protocols. Database connection parameters for a PostgreSQL server are defined, including server address, port, database name, user, and password. Docker image names for backend and frontend services are specified. Authentication settings for Auth0 include domain, client IDs, and audience URLs. AWS credentials and region are configured, along with tokens for a modal environment.

---
Made with ❤️ by [Driver](https://www.driver.ai/)