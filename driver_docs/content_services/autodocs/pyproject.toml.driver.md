# Purpose
The provided content is a configuration file for the Python dependency management tool, Poetry. It specifies the metadata and dependencies for a project named "autodocs" with version "0.1.0". The `[tool.poetry]` section includes basic project information such as the name, version, description, and authors, as well as the packages to include, specifically the "src/utils" directory. The `[tool.poetry.dependencies]` section lists the required Python version and various dependencies needed for the project, including libraries like OpenAI, Tiktoken, and Boto3, among others. Additionally, it includes local paths for development dependencies such as "database" and "shared". The `[build-system]` section indicates that the project requires "poetry-core" for building, using "poetry.core.masonry.api" as the build backend. This file is primarily used to create a virtual environment for local development, while dependencies for containers are managed separately by Modal.
