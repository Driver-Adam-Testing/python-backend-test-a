# Purpose
This file is a `pyproject.toml` configuration file for a Python project managed by Poetry. It specifies the project's metadata, including its name, version, and author, as well as its dependencies, such as Python version 3.12 and a local shared package. Additionally, it defines the build system requirements, indicating the use of `poetry-core` as the build backend.
