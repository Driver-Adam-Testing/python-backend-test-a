# Purpose
The provided Python code defines a class `AnthropicStrictAgent`, which extends the `AgentBase` class from a shared module. This class is designed to interact with the Anthropic API, a service that likely provides AI-driven functionalities such as natural language processing or machine learning model interactions. The primary purpose of this class is to manage and execute tool calls, format responses, and handle message completions in a structured manner. It encapsulates the logic for creating and sending requests to the Anthropic service, processing the responses, and managing the flow of tool execution within the context of a conversation or task.

The class includes several key methods: `_execute_tool_calls`, `_create_completion`, `_execute_iteration`, and `invoke`. `_execute_tool_calls` processes a list of tool calls, executing each tool and handling any exceptions that occur. `_create_completion` constructs a request to the Anthropic API, including model specifications and message content, and retrieves a response. `_execute_iteration` manages the iterative process of executing tool calls and updating messages based on the API's response. The `invoke` method serves as the public interface for initiating a prompt, optionally formatting the response according to a specified schema. This class is likely part of a larger system where it acts as a specialized agent for handling specific tasks using the Anthropic API, providing a structured and automated way to interact with AI tools.
# Imports and Dependencies

---
- `json`
- `anthropic`
- `shared.agent.agent_base`


# Classes

---
### AnthropicStrictAgent 
- **Type**: `class`
- **Members**:
    - `response_format`: Stores the response format for the agent.
    - `client`: An instance of the Anthropic client used for message creation.
- **Description**: The `AnthropicStrictAgent` class extends the `AgentBase` class and is designed to interact with the Anthropic API to execute tool calls and create message completions. It manages the execution of tool calls by aggregating them into a single user message and handles the formatting of responses based on a specified schema. The class is responsible for creating message completions using the Anthropic client and executing iterations to process tool calls, ensuring that the responses are formatted correctly if a response format is provided.
- **Inherits From**:
    - AgentBase

**Methods**

---
#### AnthropicStrictAgent.__init__
The `__init__` function initializes an instance of the `AnthropicStrictAgent` class, setting up a response format and a client for interacting with the Anthropic API.
- **Inputs**:
    - `*args`: Variable length argument list passed to the superclass initializer.
    - `**kwargs`: Keyword arguments, from which 'response_format' is extracted and the rest are passed to the superclass initializer.
- **Control Flow**:
    - Extracts 'response_format' from kwargs if it exists, otherwise sets it to None.
    - Initializes the 'response_format' attribute with the extracted value.
    - Creates an instance of the Anthropic client and assigns it to the 'client' attribute.
    - Calls the superclass's __init__ method with the remaining args and kwargs.
- **Output**:
    - This function does not return any value; it initializes the instance attributes.


---
#### AnthropicStrictAgent._create_completion
The `_create_completion` function constructs a dictionary of parameters for a completion request and sends it to the Anthropic client to generate a response.
- **Inputs**:
    - None
- **Control Flow**:
    - Initialize an empty dictionary `completion_kwargs` to store parameters for the completion request.
    - Set the 'model' key in `completion_kwargs` to the instance's `model` attribute.
    - Set the 'messages' key in `completion_kwargs` to the instance's `messages` attribute.
    - Set the 'max_tokens' key in `completion_kwargs` to 2048.
    - Check if the instance has any tools; if so, add a 'tools' key to `completion_kwargs` with a list of tool schemas and set 'tool_choice' to 'auto'.
    - Concatenate all system messages from `self.messages` and add them to `completion_kwargs` under the 'system' key.
    - Print the `completion_kwargs` dictionary for debugging purposes.
    - Call the `create` method on `self.client.messages` with `completion_kwargs` as arguments to generate a response.
    - Return the response from the client.
- **Output**:
    - The function returns a response object generated by the Anthropic client based on the constructed `completion_kwargs`.


---
#### AnthropicStrictAgent._execute_iteration
The `_execute_iteration` function processes a completion response to identify and execute tool calls, returning a boolean indicating if any tool calls were executed.
- **Inputs**:
    - None
- **Control Flow**:
    - Call the `_create_completion` method to generate a response and add the first message from the response to the agent's messages.
    - Initialize an empty list `tool_calls` to store tool call information.
    - Iterate over the content of the first message in the response, checking for items of type 'tool_use'.
    - For each 'tool_use' item, extract its 'id', 'name', and 'input', and append this information as a dictionary to the `tool_calls` list.
    - If `tool_calls` is not empty, call the `_execute_tool_calls` method with `tool_calls` as an argument and return `True`.
    - If no tool calls are found, return `False`.
- **Output**:
    - A boolean value indicating whether any tool calls were executed (True if executed, False otherwise).


---
#### AnthropicStrictAgent._execute_tool_calls
The `_execute_tool_calls` function processes a list of tool calls, executing each tool and collecting their results into a single user message.
- **Inputs**:
    - `tool_calls`: A list of dictionaries, each representing a tool call with keys 'id', 'name', and 'input'.
- **Control Flow**:
    - Initialize a message dictionary with 'role' set to 'user' and an empty 'content' list.
    - Iterate over each tool call in the `tool_calls` list.
    - For each tool call, create a `tool_message` dictionary with default 'content' set to 'Error in tool call'.
    - Attempt to find the tool in `self.tools` that matches the 'name' in the tool call.
    - If the tool is found, execute it with the provided input and update the 'content' of `tool_message` with the result.
    - If an exception occurs during tool execution, update the 'content' of `tool_message` with the error message.
    - Append the `tool_message` to the 'content' list of the `message` dictionary.
    - After processing all tool calls, add the `message` to the agent's messages using `self.add_message()`.
- **Output**:
    - The function does not return a value; it updates the agent's messages with the results of the tool calls.


---
#### AnthropicStrictAgent.invoke
The `invoke` function processes a prompt and optionally formats the response according to a specified schema.
- **Inputs**:
    - `prompt`: A string input that represents the prompt to be processed by the function.
- **Control Flow**:
    - The function calls the `invoke` method of its superclass with the provided prompt to get an initial response.
    - It checks if a `response_format` is specified; if so, it constructs a new prompt to format the response according to the schema defined by `response_format`.
    - The function uses the `client.completions.create` method to generate a formatted response based on the constructed prompt.
    - The formatted response is parsed from JSON and returned as an object of the type specified by `response_format`.
    - If no `response_format` is specified, the function simply returns the initial response.
- **Output**:
    - The function returns either the initial response or a formatted response object, depending on whether a `response_format` is specified.



