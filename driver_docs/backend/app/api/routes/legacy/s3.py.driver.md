# Purpose
The provided Python code defines a class `S3BucketAccess` that facilitates interaction with Amazon S3 storage. This class is designed to handle file operations such as uploading, retrieving, and deleting files within an S3 bucket. The class is initialized with an organization ID, a primary asset ID, and an optional version ID, which are used to construct the S3 bucket name and file paths. The organization ID is hashed to create a unique bucket name, ensuring privacy and consistency. The class uses the `boto3` library to interact with AWS S3, and it requires AWS credentials and configuration settings, which are imported from an external configuration module.

The `S3BucketAccess` class provides a focused set of functionalities centered around file management in S3, making it a utility class rather than a broad library. It includes methods for uploading files (`upload_file`), retrieving file content (`get_file_content`), and deleting files (`delete_file`). The class also includes a private method `_hash_organization_id` to hash the organization ID for bucket naming. The code is structured to be used as part of a larger application, likely imported and instantiated where S3 file operations are needed. It does not define a public API or external interface beyond the class methods, and it relies on logging to provide feedback on operations such as file deletions.
# Imports and Dependencies

---
- `hashlib`
- `logging`
- `boto3`
- `app.core.config.settings`


# Global Variables

---
### logger 
- **Type**: `logging.Logger`
- **Description**: The `logger` variable is an instance of Python's `logging.Logger` class, initialized with the module's `__name__` as its name. This allows for logging messages that are associated with the module's namespace, which is useful for tracking and debugging purposes.
- **Use**: The `logger` is used to log informational messages, such as when a file is deleted from an S3 bucket.


# Classes

---
### S3BucketAccess 
- **Type**: `class`
- **Members**:
    - `s3_client`: An instance of the boto3 S3 client configured with AWS credentials and settings.
    - `organization_id_hashed`: A hashed version of the organization ID used as the S3 bucket name.
    - `primary_asset_id`: The primary asset identifier used in constructing S3 file paths.
    - `version_id`: An optional version identifier used in constructing S3 file paths.
- **Description**: The `S3BucketAccess` class provides an interface for interacting with an S3 bucket, allowing for file uploads, retrievals, and deletions. It initializes an S3 client using AWS credentials and settings, and uses a hashed organization ID as the bucket name. The class includes methods to construct file paths, upload files to the S3 bucket, retrieve file contents, and delete files from the bucket. It handles both text and binary file content, with specific handling for PDF files.

**Methods**

---
#### S3BucketAccess.__init__
The `__init__` function initializes an instance of the `S3BucketAccess` class by setting up an S3 client and storing hashed organization ID, primary asset ID, and version ID.
- **Inputs**:
    - `organization_id`: A string representing the organization ID, which will be hashed for use as the S3 bucket name.
    - `primary_asset_id`: A string representing the primary asset ID, used in constructing file paths.
    - `version_id`: An optional string representing the version ID, also used in constructing file paths.
- **Control Flow**:
    - Initialize an S3 client using credentials and settings from the `settings` module.
    - Hash the `organization_id` using the `_hash_organization_id` method and store the result in `self.organization_id_hashed`.
    - Store the `primary_asset_id` in `self.primary_asset_id`.
    - Store the `version_id` in `self.version_id`.
- **Output**:
    - The function does not return any value; it initializes the instance variables for the class.


---
#### S3BucketAccess._hash_organization_id
The function `_hash_organization_id` hashes an organization ID using SHA-256 and returns the first 63 characters of the hash.
- **Inputs**:
    - `organization_id`: A string representing the organization ID to be hashed.
- **Control Flow**:
    - The function encodes the `organization_id` string to bytes.
    - It then computes the SHA-256 hash of the encoded bytes.
    - The hexadecimal representation of the hash is obtained.
    - The function returns the first 63 characters of this hexadecimal hash.
- **Output**:
    - A string that is the first 63 characters of the SHA-256 hash of the input `organization_id`.


---
#### S3BucketAccess.delete_file
The `delete_file` function removes a specified file from an S3 bucket associated with a hashed organization ID.
- **Inputs**:
    - `relative_path`: A string representing the relative path of the file to be deleted from the S3 bucket.
- **Control Flow**:
    - The function first calls `get_file_path` with `relative_path` to construct the full file path in the S3 bucket.
    - It then uses the `s3_client` to call `delete_object`, specifying the bucket name as `organization_id_hashed` and the key as the constructed file path.
    - Finally, it logs an informational message indicating that the file has been deleted from the specified bucket.
- **Output**:
    - The function does not return any value; it performs a side effect by deleting a file from an S3 bucket and logging the action.


---
#### S3BucketAccess.get_file_content
The `get_file_content` function retrieves the content of a specified file from an S3 bucket, decoding it if necessary.
- **Inputs**:
    - `relative_path`: A string representing the relative path of the file within the S3 bucket.
- **Control Flow**:
    - The function first constructs the full file path using the `get_file_path` method with the provided `relative_path`.
    - It attempts to retrieve the file object from the S3 bucket using the `s3_client.get_object` method with the bucket name and file path.
    - If the file is successfully retrieved, it reads the file content from the object's body.
    - If the file is a PDF (determined by checking if the `relative_path` ends with ".pdf"), it returns the raw file content.
    - If the file is not a PDF, it decodes the file content using UTF-8 encoding, replacing any errors, and returns the decoded content.
    - If the file does not exist (caught by `NoSuchKey` exception), it prints an error message and returns an empty string.
- **Output**:
    - The function returns the content of the file as a string, either raw or UTF-8 decoded, or an empty string if the file does not exist.


---
#### S3BucketAccess.get_file_path
The `get_file_path` function constructs a full file path by combining the primary asset ID, version ID, and a given relative path, ensuring the relative path does not start with a slash.
- **Inputs**:
    - `relative_path`: A string representing the relative path of the file, which may include a leading slash.
- **Control Flow**:
    - The function takes a single input, `relative_path`, which is a string.
    - It uses the `lstrip('/')` method on `relative_path` to remove any leading slashes.
    - The function constructs a string by concatenating `self.primary_asset_id`, `self.version_id`, and the modified `relative_path`, separated by slashes.
    - The constructed string is returned as the full file path.
- **Output**:
    - A string representing the full file path, constructed by combining the primary asset ID, version ID, and the relative path without a leading slash.


---
#### S3BucketAccess.upload_file
The `upload_file` function uploads a file to an S3 bucket using a specified file path and relative path.
- **Inputs**:
    - `file_path`: The local file path of the file to be uploaded.
    - `relative_path`: The relative path within the S3 bucket where the file should be stored.
- **Control Flow**:
    - The function calls `get_file_path` with `relative_path` to generate the S3 path where the file will be stored.
    - It then uses the `s3_client` to upload the file from `file_path` to the S3 bucket, using the hashed organization ID as the bucket name and the generated S3 path as the key.
- **Output**:
    - The function does not return any value; it performs the side effect of uploading a file to an S3 bucket.



