## Folders
- **[modal-deploy](actions/modal-deploy.driver.md)**: The `modal-deploy` folder in the `python-backend` codebase contains a GitHub Action configuration file, `action.yml`, which outlines the process for deploying a modal package, including dependency installation, testing, and deployment steps.

