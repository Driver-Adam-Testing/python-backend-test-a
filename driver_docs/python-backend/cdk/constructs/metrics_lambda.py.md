<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `metrics_lambda.py` file defines a CDK construct for deploying a Lambda function that processes metrics events, along with associated resources such as an event bus, dead letter queue, and CloudWatch alarms for monitoring in different environments.

# Purpose
This Python code defines a module that leverages the AWS Cloud Development Kit (CDK) to set up a serverless infrastructure for processing metrics using AWS Lambda, EventBridge, and other AWS services. The primary class, `MetricsLambda`, is a construct that encapsulates the creation and configuration of an AWS Lambda function designed to handle metric events. It integrates with AWS Secrets Manager to manage database credentials, AWS SSM for retrieving VPC information, and AWS SQS for handling dead-letter queues. The Lambda function is configured to run in a specified VPC and is set up with specific environment variables and bundling options to ensure it operates correctly within the AWS environment.

The module also includes functionality for monitoring and alerting using AWS CloudWatch. It sets up alarms to monitor the dead-letter queue for undelivered messages and the age of messages, as well as the error rate of the Lambda function. These alarms are configured to trigger notifications via SNS if a CloudWatch alarm ARN is provided. This code is intended to be part of a larger infrastructure setup, likely imported and used within a broader CDK application to automate the deployment and management of AWS resources for metric processing.
# Imports and Dependencies

---
- `os`
- `aws_cdk.Duration`
- `aws_cdk.aws_cloudwatch`
- `aws_cdk.aws_cloudwatch_actions`
- `aws_cdk.aws_ec2`
- `aws_cdk.aws_events`
- `aws_cdk.aws_lambda`
- `aws_cdk.aws_lambda_python_alpha`
- `aws_cdk.aws_secretsmanager`
- `aws_cdk.aws_sns`
- `aws_cdk.aws_sqs`
- `aws_cdk.aws_ssm`
- `aws_cdk.aws_events_targets`
- `constructs.Construct`


# Classes

---
### MetricsLambdaParams<!-- {{#class:python-backend/cdk/constructs/metrics_lambda.MetricsLambdaParams}} -->
- **Members**:
    - `environment`: Specifies the environment in which the lambda function is running.
    - `database_url`: Holds the URL of the database, if provided.
    - `cloudwatch_alarm_arn`: Contains the ARN for a CloudWatch alarm, if specified.
- **Description**: The `MetricsLambdaParams` class is a simple data structure used to encapsulate parameters required for configuring a metrics-related AWS Lambda function. It includes the environment name, an optional database URL, and an optional CloudWatch alarm ARN, which are used to set up the environment and monitoring for the Lambda function.
- **Methods**:
    - [`python-backend/cdk/constructs/metrics_lambda.MetricsLambdaParams.__init__`](#MetricsLambdaParams__init__)

**Methods**

---
#### MetricsLambdaParams\.\_\_init\_\_<!-- {{#callable:python-backend/cdk/constructs/metrics_lambda.MetricsLambdaParams.__init__}} -->
The `__init__` method initializes an instance of the `MetricsLambdaParams` class with environment, database URL, and CloudWatch alarm ARN parameters.
- **Inputs**:
    - `environment`: A string representing the environment in which the metrics lambda is operating.
    - `database_url`: An optional string representing the URL of the database; defaults to None if not provided.
    - `cloudwatch_alarm_arn`: An optional string representing the ARN of the CloudWatch alarm; defaults to None if not provided.
- **Control Flow**:
    - Assigns the provided 'environment' argument to the instance variable 'self.environment'.
    - Assigns the provided 'database_url' argument to the instance variable 'self.database_url'.
    - Assigns the provided 'cloudwatch_alarm_arn' argument to the instance variable 'self.cloudwatch_alarm_arn'.
- **Output**: The method does not return any value; it initializes the instance variables with the provided arguments.
- **See also**: [`python-backend/cdk/constructs/metrics_lambda.MetricsLambdaParams`](#MetricsLambdaParams)  (Base Class)



---
### MetricsLambda<!-- {{#class:python-backend/cdk/constructs/metrics_lambda.MetricsLambda}} -->
- **Members**:
    - `lambda_function`: An AWS Lambda function configured with specific runtime, environment, and bundling options.
    - `metrics_dlq`: An SQS queue used as a dead-letter queue for the event bus.
    - `metrics_bus`: An EventBus for handling metrics events with a dead-letter queue.
    - `metrics_rule`: An event rule that triggers the Lambda function based on specific event patterns.
    - `metric_dlq_alarm`: A CloudWatch alarm monitoring the number of undelivered messages in the DLQ.
    - `metric_message_age_alarm`: A CloudWatch alarm monitoring the age of the oldest message in the DLQ.
    - `lambda_error_rate_alarm`: A CloudWatch alarm monitoring the error rate of the Lambda function.
- **Description**: The MetricsLambda class is a construct that sets up an AWS Lambda function to process metrics events, integrating with various AWS services such as SQS, EventBridge, and CloudWatch. It configures a Lambda function with specific runtime and environment settings, sets up an event bus and rule for triggering the function, and establishes alarms to monitor the function's performance and the state of the dead-letter queue. The class also supports notification actions for alarms if a CloudWatch alarm ARN is provided.
- **Methods**:
    - [`python-backend/cdk/constructs/metrics_lambda.MetricsLambda.__init__`](#MetricsLambda__init__)
- **Inherits From**:
    - `Construct`

**Methods**

---
#### MetricsLambda\.\_\_init\_\_<!-- {{#callable:python-backend/cdk/constructs/metrics_lambda.MetricsLambda.__init__}} -->
The [`__init__`](#MetricsLambdaParams__init__) method initializes a `MetricsLambda` construct, setting up AWS resources such as a Lambda function, event bus, SQS queue, and CloudWatch alarms based on the provided parameters.
- **Inputs**:
    - `scope`: An instance of the `Construct` class, representing the scope in which this construct is defined.
    - `id`: A string that serves as the unique identifier for this construct within its scope.
    - `params`: An instance of `MetricsLambdaParams` containing configuration parameters such as environment, database URL, and CloudWatch alarm ARN.
- **Control Flow**:
    - The method begins by calling the superclass constructor with `scope` and `id`.
    - A secret for the database URL is created using AWS Secrets Manager.
    - The VPC ID is retrieved from AWS SSM Parameter Store, and a VPC object is created using this ID.
    - The absolute path to the `driver_db` directory is determined.
    - A Python Lambda function is created with specified configurations, including VPC settings, environment variables, and bundling options.
    - The Lambda function is granted read access to the database URL secret.
    - An event target is created for the Lambda function, and an SQS queue is set up as a dead-letter queue.
    - An event bus is created with the dead-letter queue, and a rule is set up to trigger the Lambda function based on specific event patterns.
    - If the environment is 'development', 'staging', or 'production', CloudWatch alarms are created for DLQ message count, message age, and Lambda error rate.
    - If a CloudWatch alarm ARN is provided, SNS actions are added to the alarms for notifications; otherwise, a warning message is printed.
- **Output**: The method does not return any value; it initializes the `MetricsLambda` construct with the specified AWS resources and configurations.
- **Functions called**:
    - [`python-backend/cdk/constructs/metrics_lambda.MetricsLambdaParams.__init__`](#MetricsLambdaParams__init__)
- **See also**: [`python-backend/cdk/constructs/metrics_lambda.MetricsLambda`](#MetricsLambda)  (Base Class)




---
Made with ❤️ by [Driver](https://www.driver.ai/)