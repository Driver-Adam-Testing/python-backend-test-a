## Folders
- **[metrics_handler](lambdas/metrics_handler.driver.md)**: The `metrics_handler` folder in the `python-backend` codebase contains the main logic for handling AWS Lambda events, managing dependencies, and sending usage events related to LLM sessions, with configuration and dependency details specified in `pyproject.toml` and `poetry.lock` files.

