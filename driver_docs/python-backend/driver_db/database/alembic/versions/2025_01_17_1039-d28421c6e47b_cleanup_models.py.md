<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `2025_01_17_1039-d28421c6e47b_cleanup_models.py` file contains an Alembic migration script for the `python-backend` codebase that removes several database tables, columns, and constraints, and modifies indexes and foreign keys.

# Purpose
This Python file is an Alembic migration script used to manage changes to a database schema. It is part of a version control system for database schemas, allowing developers to apply and revert changes in a controlled manner. The script defines two main functions: `upgrade()` and `downgrade()`. The `upgrade()` function is responsible for applying changes to the database schema, which in this case involves dropping several indexes, foreign key constraints, columns, and entire tables such as "workspaces", "codebases", and "inspection_versions". It also creates a new foreign key constraint for the "inspectorrun" table. Conversely, the `downgrade()` function reverses these changes, effectively restoring the database schema to its previous state by recreating the dropped tables, columns, indexes, and constraints.

The script is a part of a broader database migration framework, specifically using Alembic, which is a lightweight database migration tool for use with SQLAlchemy. The script includes metadata such as the revision ID and the down revision ID, which are used by Alembic to track the order of migrations. This file is not intended to be executed as a standalone script but rather as part of a sequence of migrations managed by Alembic. It does not define public APIs or external interfaces but serves as an internal tool for database schema management within a software project.
# Imports and Dependencies

---
- `sqlalchemy`
- `alembic.op`
- `sqlalchemy.dialects.postgresql`


# Global Variables

---
### revision
- **Type**: `string`
- **Description**: The `revision` variable is a string that represents the unique identifier for the current database schema migration revision. It is used by Alembic, a database migration tool for SQLAlchemy, to track changes to the database schema over time.
- **Use**: This variable is used to identify the current migration version in the Alembic migration scripts.


---
### down\_revision
- **Type**: `string`
- **Description**: The `down_revision` variable is a string that holds the identifier of the previous database schema revision in an Alembic migration script. It is used to establish a link between the current revision and its predecessor, allowing Alembic to maintain a linear history of schema changes.
- **Use**: This variable is used by Alembic to determine the order of database migrations and ensure that they are applied in the correct sequence.


---
### branch\_labels
- **Type**: `NoneType`
- **Description**: The variable `branch_labels` is a global variable set to `None`. It is part of the Alembic migration script metadata, which typically includes information about the migration such as revision identifiers and dependencies.
- **Use**: This variable is used to define branch labels for the migration, but in this case, it is not utilized as it is set to `None`.


---
### depends\_on
- **Type**: `NoneType`
- **Description**: The `depends_on` variable is a global variable set to `None`. It is part of the Alembic migration script metadata, which typically includes information about dependencies between migration scripts.
- **Use**: This variable is used to indicate that the current migration script does not depend on any other migration scripts.


# Functions

---
### upgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2025_01_17_1039-d28421c6e47b_cleanup_models.upgrade}} -->
The `upgrade` function performs a series of database schema modifications, including dropping indexes, constraints, columns, and tables, and creating a new foreign key constraint.
- **Inputs**: None
- **Control Flow**:
    - Drop several indexes from the 'codebases' and 'derived_contents' tables.
    - Drop foreign key constraints from 'document_sources', 'inspectorrun', and 'codebases' tables.
    - Drop columns from 'runtimelogagentinstance', 'derived_contents', and 'document_sources' tables.
    - Drop several tables including 'workspaces', 'codebases', 'inspection_versions', 'tags_contents', and 'derived_content_types'.
    - Create a new index on the 'tags' table for the 'type' column.
    - Create a new foreign key constraint on the 'inspectorrun' table referencing the 'v2_version' table with 'ondelete' set to 'SET NULL'.
- **Output**: The function does not return any value; it modifies the database schema as specified.


---
### downgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2025_01_17_1039-d28421c6e47b_cleanup_models.downgrade}} -->
The `downgrade` function reverts database schema changes by recreating tables, columns, constraints, and indexes that were previously dropped.
- **Inputs**: None
- **Control Flow**:
    - The function begins by creating the 'derived_content_types' table with columns for 'id', 'type_name', 'created_at', and 'updated_at', along with primary and unique constraints.
    - It then creates the 'tags_contents' table with columns for 'tag_id', 'content_id', and 'include', and sets up foreign key constraints linking to 'derived_contents' and 'tags'.
    - The 'inspection_versions' table is created next, with columns for 'id', 'version', 'display_name', 'created_at', 'updated_at', and 'previous_version_id', along with a primary key constraint.
    - The 'codebases' table is recreated with columns for 'id', 'workspace_id', 'codebase_name', 'description', 'status', 'storage_url', 'resource_root', 'creator_id', 'created_at', and 'updated_at', including foreign key and unique constraints.
    - The 'workspaces' table is recreated with columns for 'id', 'display_name', 'description', 'organization_id', 'created_at', and 'updated_at', along with a primary key constraint.
    - Indexes are created on the 'codebases' and 'derived_contents' tables for various columns to optimize query performance.
    - The function modifies the 'tags' table by dropping an existing index and creating a new one on the 'type' column.
    - Columns 'workspace_id' and 'codebase_id' are added to the 'runtimelogagentinstance' table.
    - A foreign key constraint is dropped from the 'inspectorrun' table and a new one is created linking 'version_id' to 'v2_version'.
    - Columns 'source_id', 'include', and 'document_id' are added to the 'document_sources' table, with foreign key constraints linking to 'derived_contents'.
    - Columns 'workspace_id', 'status', and 'codebase_id' are added to the 'derived_contents' table, and several indexes are created on this table for different columns.
- **Output**: The function does not return any value; it performs database schema modifications as a side effect.



---
Made with ❤️ by [Driver](https://www.driver.ai/)