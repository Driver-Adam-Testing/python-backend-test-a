# Purpose
This Python code file is designed to define a template for analyzing and structuring source code in a multi-context environment. It imports several components from utility modules that specialize in language processing and code analysis, such as collections for data structures, functions, and variables, as well as prompts for system and user interactions. The primary purpose of this file is to provide a structured template, `SOURCE_CODE_MULTI_CONTEXT_TEMPLATE_DEFAULT`, which outlines various aspects of source code, including its purpose, imports and dependencies, global variables, data structures, and functions. Each component of the template is associated with specific prompts or functions that facilitate the extraction and organization of these elements from the source code.

The file is not a standalone script but rather a configuration or template definition that is likely intended to be used within a larger system or framework for code analysis. It does not define public APIs or external interfaces directly but instead relies on imported utilities to perform its tasks. The template is structured to handle multiple contexts, suggesting its use in complex or large-scale codebases where understanding the interplay between different parts of the code is crucial. The use of lambda functions and conditional JSON processing indicates a flexible approach to handling the output of language models or other analytical tools, making it adaptable to various scenarios in code analysis.
# Imports and Dependencies

---
- `utils.lang_specialization.default`
- `utils.lang_specialization.default_multi_context`
- `utils.templates`


# Global Variables

---
### SOURCE_CODE_MULTI_CONTEXT_TEMPLATE_DEFAULT 
- **Type**: `list`
- **Description**: `SOURCE_CODE_MULTI_CONTEXT_TEMPLATE_DEFAULT` is a list of tuples, each containing a set of parameters used for processing different aspects of source code in a multi-context environment. Each tuple includes a type identifier, a description, and a series of functions or constants that are used to handle specific code elements such as prompts, imports, global variables, data structures, and functions.
- **Use**: This variable is used to define a template for processing and analyzing source code across multiple contexts, facilitating the extraction and handling of various code components.


