## Folders
- **[src](pdf_preprocessing/src.driver.md)**: The `src` folder in the `python-backend` codebase contains the `main.py` file, which is responsible for processing PDF files, including downloading, sanitizing, embedding summaries, persisting results to a database, and handling exceptions with notification emails.

## Files
- **[.gitignore](pdf_preprocessing/.gitignore.driver.md)**: The `.gitignore` file in the `python-backend` codebase specifies that all files within the `metadata` and `pdfs` directories should be ignored by Git.
- **[poetry.lock](pdf_preprocessing/poetry.lock.driver.md)**: The `poetry.lock` file in the `python-backend` codebase is a crucial component for managing Python project dependencies, ensuring consistent and reproducible builds by locking specific package versions, detailing package metadata, dependencies, optional features, file hashes, and platform-specific files, while also providing metadata for integrity and compatibility across different environments.
- **[pyproject.toml](pdf_preprocessing/pyproject.toml.driver.md)**: The `pyproject.toml` file in the `python-backend` codebase specifies the project metadata, dependencies, and build system configuration for the `pdf-preprocessing` package.
- **[README.md](pdf_preprocessing/README.md.driver.md)**: Empty file (no analyzable contents).
