<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

Alembic migration script to clean up database models by dropping and recreating tables, columns, and constraints.

# Purpose
This code is a database migration script using Alembic, a database migration tool for SQLAlchemy. The script defines two main functions: [`upgrade`](<#upgrade>) and [`downgrade`](<#downgrade>). The [`upgrade`](<#upgrade>) function is responsible for applying changes to the database schema, which includes dropping several indexes and constraints, removing columns from tables, and dropping entire tables such as `workspaces`, `codebases`, `inspection_versions`, `tags_contents`, and `derived_content_types`. It also creates a new foreign key constraint on the `inspectorrun` table. These operations are part of a schema cleanup process, likely intended to remove obsolete or redundant database structures.

The [`downgrade`](<#downgrade>) function reverses the changes made by the [`upgrade`](<#upgrade>) function. It recreates the tables, columns, indexes, and constraints that were removed during the upgrade. This function ensures that the database schema can be reverted to its previous state if necessary. The script uses SQLAlchemy's data types and Alembic's operations to define the schema changes, ensuring compatibility with PostgreSQL. The script includes revision identifiers, which Alembic uses to track the migration history and dependencies between different schema versions.
# Imports and Dependencies

---
- `sqlalchemy`
- `alembic.op`
- `sqlalchemy.dialects.postgresql`


# Global Variables

---
### revision
- **Type**: ``str``
- **Description**: Contains the unique identifier for the current database schema revision in the Alembic migration script. This identifier is used to track the specific state of the database schema at a given point in time.
- **Use**: Used by Alembic to identify the current revision of the database schema for migration purposes.


---
### down\_revision
- **Type**: ``str``
- **Description**: Specifies the identifier of the previous database schema revision in an Alembic migration script. This identifier is used to track the sequence of database schema changes.
- **Use**: Used by Alembic to determine the order of migrations and to apply them correctly.


---
### branch\_labels
- **Type**: ``NoneType``
- **Description**: `branch_labels` is a global variable set to `None`. It is part of the Alembic migration script metadata.
- **Use**: Indicates that there are no branch labels associated with this migration script.


---
### depends\_on
- **Type**: ``NoneType``
- **Description**: Represents a global variable that is set to `None`. It is used as a placeholder for dependencies in Alembic migration scripts.
- **Use**: Indicates that there are no dependencies for the current migration script.


# Functions

---
### upgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2025_01_17_1039-d28421c6e47b_cleanup_models.upgrade}} -->
[View Source →](<../../../../../../driver_db/database/alembic/versions/2025_01_17_1039-d28421c6e47b_cleanup_models.py#L20>)

Modifies the database schema by dropping specific indexes, constraints, columns, and tables, and creating a new foreign key.
- **Inputs**: None
- **Logic and Control Flow**:
    - Drops several indexes from the 'codebases' and 'derived_contents' tables using `op.drop_index`.
    - Removes foreign key constraints from 'document_sources', 'inspectorrun', and 'codebases' tables using `op.drop_constraint`.
    - Drops columns from 'runtimelogagentinstance', 'derived_contents', and 'document_sources' tables using `op.drop_column`.
    - Drops several tables including 'workspaces', 'codebases', 'inspection_versions', 'tags_contents', and 'derived_content_types' using `op.drop_table`.
    - Creates a new index on the 'tags' table using `op.create_index`.
    - Creates a new foreign key on the 'inspectorrun' table referencing 'v2_version' using `op.create_foreign_key`.
- **Output**: No return value; modifies the database schema directly.


---
### downgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2025_01_17_1039-d28421c6e47b_cleanup_models.downgrade}} -->
[View Source →](<../../../../../../driver_db/database/alembic/versions/2025_01_17_1039-d28421c6e47b_cleanup_models.py#L64>)

Recreates database tables, columns, constraints, and indexes that were previously dropped.
- **Inputs**: None
- **Logic and Control Flow**:
    - Creates the `derived_content_types` table with columns `id`, `type_name`, `created_at`, and `updated_at`, and sets primary and unique constraints.
    - Creates the `tags_contents` table with columns `tag_id`, `content_id`, and `include`, and sets primary and foreign key constraints.
    - Creates the `inspection_versions` table with columns `id`, `version`, `display_name`, `created_at`, `updated_at`, and `previous_version_id`, and sets a primary key constraint.
    - Creates the `codebases` table with columns `id`, `workspace_id`, `codebase_name`, `description`, `status`, `storage_url`, `resource_root`, `creator_id`, `created_at`, and `updated_at`, and sets primary, unique, and foreign key constraints.
    - Creates the `workspaces` table with columns `id`, `display_name`, `description`, `organization_id`, `created_at`, and `updated_at`, and sets a primary key constraint.
    - Creates an index `ix_codebases_workspace_id` on the `codebases` table for the `workspace_id` column.
    - Drops the index `ix_tags_type` on the `tags` table and creates a new index `ix_tag_type` on the `type` column.
    - Adds columns `workspace_id` and `codebase_id` to the `runtimelogagentinstance` table.
    - Drops an unnamed foreign key constraint on the `inspectorrun` table and creates a new foreign key `inspectorrun_version_id_fkey`.
    - Adds columns `source_id`, `include`, and `document_id` to the `document_sources` table and creates foreign key constraints `document_sources_document_id_fkey` and `document_sources_source_id_fkey`.
    - Adds columns `workspace_id`, `status`, and `codebase_id` to the `derived_contents` table and creates several indexes on different columns.
- **Output**: No return value; modifies the database schema.



---
Made with ❤️ by [Driver](https://www.driver.ai/)