# Purpose
The provided content is a configuration file for the Python dependency management tool, Poetry. It defines the metadata and dependencies for a project named "pdf-preprocessing" with version "0.1.0". The file specifies the project's dependencies, including Python version 3.12 and various libraries such as `requests`, `pymupdf`, and `openai`, among others. It also includes development dependencies under the `[tool.poetry.group.dev.dependencies]` section, specifically listing `pytest` for testing purposes. Additionally, the `[build-system]` section indicates that the project requires `poetry-core` for building, using `poetry.core.masonry.api` as the build backend. This configuration ensures that the project environment is correctly set up with all necessary packages for both development and production.
