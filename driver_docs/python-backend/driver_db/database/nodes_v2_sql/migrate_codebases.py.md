<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `migrate_codebases.py` file contains a SQL script for migrating codebase data, including preparing and inserting primary assets and versions, inserting nodes, and updating derived contents in a database.

# Purpose
The provided code is a SQL script designed to migrate and organize data related to codebases and their versions within a database. It performs a series of operations to prepare and insert primary assets and their versions, insert nodes representing directories and files, and update derived content records to link them with the newly created nodes. The script begins by creating temporary tables to resolve version IDs and mark primary assets, then inserts data into the `v2_primary_asset` and `v2_version` tables to establish a structured representation of codebases and their versions. This process ensures that each codebase is associated with a primary asset ID and a display name, and that versions are linked to their respective primary assets.

Subsequently, the script focuses on inserting nodes into the `v2_node` table, which represent individual directories and files within the codebases. It deduplicates nodes by ensuring that only the most recent entries are considered, and assigns appropriate node kinds based on the content type. The script then updates the `derived_contents` table to associate each content entry with its corresponding node, effectively linking child and top-level content to the newly created nodes. This comprehensive migration process culminates in an optional final selection of all nodes, ordered by their creation date, to verify the successful organization and integration of the data. The script is structured to be executed as a transaction, ensuring atomicity and consistency throughout the migration process.
# Global Variables

---
### MIGRATE\_CODEBASE
- **Type**: `str`
- **Description**: The `MIGRATE_CODEBASE` variable is a multi-line string containing a SQL script. This script is designed to migrate a codebase by preparing and inserting primary assets and versions, inserting nodes, and updating derived contents. It involves creating temporary tables, inserting data into new tables, and updating existing records to reflect the new structure.
- **Use**: This variable is used to store and execute a SQL script for migrating a codebase within a database.



---
Made with ❤️ by [Driver](https://www.driver.ai/)