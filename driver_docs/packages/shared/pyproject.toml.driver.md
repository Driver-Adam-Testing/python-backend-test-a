# Purpose
The provided content is a configuration file for the Python dependency management tool, Poetry. It defines the metadata and dependencies for a Python project named "shared" with version "0.1.0". The file specifies the project's author, Neil Zumwalde, and indicates that the project's documentation can be found in "README.md". The `[tool.poetry.dependencies]` section lists the required runtime dependencies, including specific versions of Python, markdown, and other libraries such as boto3 and openai. Additionally, the `[tool.poetry.group.dev.dependencies]` section specifies development dependencies, such as pytest for testing. The `[build-system]` section outlines the build requirements, indicating that the project uses "poetry-core" as its build system backend. This configuration ensures that the project environment is consistently set up with the correct dependencies for both development and production.
