<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `maximum_iterations.py` file defines functions to render a prompt and a message indicating the maximum number of iterations allowed to solve a user's problem.

# Purpose
This code provides a narrow functionality focused on generating a formatted message string and packaging it into a dictionary. It consists of a template string `PROMPT` that includes a placeholder for the number of iterations, and two functions: [`render_prompt`](#render_prompt) and [`render_message`](#render_message). The [`render_prompt`](#render_prompt) function takes an integer `max_iterations` as an argument and returns the `PROMPT` string with the placeholder replaced by the provided integer. The [`render_message`](#render_message) function builds upon this by returning a dictionary with a fixed key-value pair, where the value is the formatted prompt string generated by [`render_prompt`](#render_prompt). This code is likely part of a larger system where such formatted messages are used for communication or logging purposes.
# Global Variables

---
### PROMPT
- **Type**: `str`
- **Description**: The `PROMPT` variable is a string template that includes a placeholder for `max_iterations`. It is used to communicate the maximum number of iterations allowed to solve a user's problem.
- **Use**: This variable is used as a template to generate a formatted string that specifies the iteration limit in user-facing messages.


# Functions

---
### render\_prompt<!-- {{#callable:python-backend/packages/shared/shared/prompts/interface/maximum_iterations.render_prompt}} -->
The function `render_prompt` formats a predefined prompt string with a specified maximum number of iterations.
- **Inputs**:
    - `max_iterations`: An integer representing the maximum number of iterations to be included in the prompt.
- **Control Flow**:
    - The function takes the input `max_iterations` and converts it to a string.
    - It uses the `format` method to insert the string representation of `max_iterations` into the `PROMPT` template.
- **Output**: A formatted string that includes the specified maximum number of iterations.


---
### render\_message<!-- {{#callable:python-backend/packages/shared/shared/prompts/interface/maximum_iterations.render_message}} -->
The `render_message` function generates a dictionary with a system role and a content message formatted with a specified number of maximum iterations.
- **Inputs**:
    - `max_iterations`: An integer representing the maximum number of iterations to be included in the message content.
- **Control Flow**:
    - Calls the [`render_prompt`](#render_prompt) function with `max_iterations` as an argument to generate a formatted string.
    - Returns a dictionary with 'role' set to 'system' and 'content' set to the result of [`render_prompt`](#render_prompt).
- **Output**: A dictionary with keys 'role' and 'content', where 'role' is 'system' and 'content' is a formatted string indicating the maximum iterations.
- **Functions called**:
    - [`python-backend/packages/shared/shared/prompts/interface/maximum_iterations.render_prompt`](#render_prompt)



---
Made with ❤️ by [Driver](https://www.driver.ai/)