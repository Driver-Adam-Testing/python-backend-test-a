## Folders
- **[files](prompt_templates/files.driver.md)**: The `files` folder in the `python-backend` codebase contains a collection of text files and a subfolder named `templates`, which together provide templates and guidelines for inspecting, analyzing, and documenting software code and metadata across various programming languages and contexts.
- **[folders](prompt_templates/folders.driver.md)**: The `folders` folder in the `python-backend` codebase contains various text files that serve as templates and instructions for summarizing software folders, focusing on creating single sentence or paragraph descriptions based on child files and folders or chunk descriptions.
- **[toplevel](prompt_templates/toplevel.driver.md)**: The `toplevel` folder in the `python-backend` codebase contains a `templates` folder with a guide template and various text files providing instructions for summarizing and describing software codebases at different levels of detail.

