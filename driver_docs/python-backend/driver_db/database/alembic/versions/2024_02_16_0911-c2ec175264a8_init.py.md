<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `2024_02_16_0911-c2ec175264a8_init.py` file in the `python-backend` codebase contains an Alembic migration script that creates tables and indexes for managing content processing sessions, content metadata, and chunks, along with their associated database extensions and constraints.

# Purpose
This Python file is an Alembic migration script used to manage changes to a database schema over time. It defines both an [`upgrade`](<#upgrade>) function and a [`downgrade`](<#downgrade>) function, which are standard in Alembic migrations to apply and revert schema changes, respectively. The script is specifically designed to create and manage tables and extensions within a PostgreSQL database. It creates three tables: `contentprocessingsession`, `contentmetadata`, and `chunk`, each with specific columns and constraints. The `contentmetadata` table includes a foreign key relationship to the `contentprocessingsession` table, and the `chunk` table has a foreign key relationship to the `contentmetadata` table. Additionally, the script creates several indexes on the `contentmetadata` table to optimize query performance.

The script also includes commands to ensure the existence of PostgreSQL extensions `vector` and `uuid-ossp`, which are used for vector operations and UUID generation, respectively. The use of the `pgvector` library indicates that the database schema supports vector-based operations, likely for machine learning or data processing tasks. The presence of SQLModel and SQLAlchemy components suggests that this migration is part of a larger application that uses these libraries for ORM (Object-Relational Mapping) functionality. This script is a crucial part of the database management process, ensuring that the database schema is correctly set up and can be modified as the application evolves.
# Imports and Dependencies

---
- `collections.abc.Sequence`
- `pgvector`
- `sqlalchemy`
- `sqlmodel`
- `alembic.op`


# Global Variables

---
### revision
- **Type**: `str`
- **Description**: The `revision` variable is a string that represents the unique identifier for the current database schema revision in an Alembic migration script. It is used to track the specific version of the database schema that this migration script applies to.
- **Use**: This variable is used by Alembic to identify and apply the correct database schema changes during the migration process.


---
### down\_revision
- **Type**: `str`
- **Description**: The `down_revision` variable is a string that holds the identifier of the previous database schema revision in an Alembic migration script. It is used to establish a linear sequence of migrations by indicating which revision this migration is based on.
- **Use**: This variable is used by Alembic to determine the order of database migrations and ensure that they are applied in the correct sequence.


---
### branch\_labels
- **Type**: `str | Sequence[str] | None`
- **Description**: The `branch_labels` variable is a global variable that can hold a string, a sequence of strings, or be set to None. It is used in the context of Alembic, a database migration tool for SQLAlchemy, to potentially label a branch in a database migration script.
- **Use**: This variable is used to specify labels for a branch in a database migration, allowing for more organized and identifiable migration paths.


---
### depends\_on
- **Type**: `str | Sequence[str] | None`
- **Description**: The `depends_on` variable is a global variable that can hold a string, a sequence of strings, or be set to None. It is used in the context of Alembic migrations to specify dependencies between different database schema revisions.
- **Use**: This variable is used to define which other revisions the current revision depends on, helping to manage the order of database migrations.


# Functions

---
### upgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_02_16_0911-c2ec175264a8_init.upgrade}} -->
The `upgrade` function applies database schema changes by creating extensions, tables, and indexes using Alembic and SQLAlchemy.
- **Inputs**: None
- **Control Flow**:
    - Execute SQL command to create the 'vector' extension if it does not exist.
    - Execute SQL command to create the 'uuid-ossp' extension if it does not exist.
    - Create the 'contentprocessingsession' table with columns 'id', 'code_revision', and 'arguments', and set 'id' as the primary key.
    - Create the 'contentmetadata' table with columns 'id', 'workspace_id', 'codebase_id', 'content_type', 'relative_path', 'misc_metadata', and 'content_processing_session_id', with 'id' as the primary key and a foreign key constraint on 'content_processing_session_id'.
    - Create indexes on 'contentmetadata' for 'codebase_id', 'relative_path', and 'workspace_id'.
    - Create the 'chunk' table with columns 'id', 'content_metadata_id', 'text', 'text_embedding_3_small', and 'chunk_number', with 'id' as the primary key and a foreign key constraint on 'content_metadata_id'.
- **Output**: The function does not return any output as it is designed to perform database schema modifications.


---
### downgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_02_16_0911-c2ec175264a8_init.downgrade}} -->
The `downgrade` function reverses database schema changes by dropping specific tables and indexes.
- **Inputs**: None
- **Control Flow**:
    - The function begins by dropping the 'chunk' table using `op.drop_table`.
    - It then drops the index on 'workspace_id' in the 'contentmetadata' table using `op.drop_index`.
    - Next, it drops the index on 'relative_path' in the 'contentmetadata' table using `op.drop_index`.
    - It proceeds to drop the index on 'codebase_id' in the 'contentmetadata' table using `op.drop_index`.
    - The function then drops the 'contentmetadata' table using `op.drop_table`.
    - Finally, it drops the 'contentprocessingsession' table using `op.drop_table`.
- **Output**: The function does not return any output; it performs database schema modifications.



---
Made with ❤️ by [Driver](https://www.driver.ai/)