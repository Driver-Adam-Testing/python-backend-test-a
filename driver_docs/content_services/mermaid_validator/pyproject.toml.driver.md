# Purpose
This file is a `pyproject.toml` configuration file for a Python project managed by Poetry. It specifies the project's metadata, including its name, version, author, and a reference to the README file. It also defines the project's dependencies, requiring Python version 3.12 or higher and the `modal` package, and sets up the build system to use `poetry-core` as the build backend.
