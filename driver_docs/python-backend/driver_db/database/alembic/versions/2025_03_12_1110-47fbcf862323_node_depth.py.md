<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `2025_03_12_1110-47fbcf862323_node_depth.py` file is an Alembic migration script that adds a computed "depth" column to the "v2_node" table and creates an index on this column.

# Purpose
This code is an Alembic migration script for a SQLAlchemy-managed database, providing narrow functionality focused on schema evolution. It introduces a new column named "depth" to the "v2_node" table, which is computed based on the structure of a "relative_path" column, effectively calculating the depth of a node in a hierarchical path. The script also creates an index on this new "depth" column to optimize query performance. The [`upgrade`](<#upgrade>) function applies these changes, while the [`downgrade`](<#downgrade>) function reverses them, ensuring that the database schema can be rolled back if necessary. This script is part of a version-controlled database migration process, facilitating incremental updates to the database schema.
# Imports and Dependencies

---
- `sqlalchemy`
- `alembic.op`


# Global Variables

---
### revision
- **Type**: `string`
- **Description**: The `revision` variable is a string that holds the unique identifier for the current database schema migration. It is used by Alembic, a database migration tool for SQLAlchemy, to track and apply changes to the database schema.
- **Use**: This variable is used to identify the current migration version in the Alembic migration script.


---
### down\_revision
- **Type**: `str`
- **Description**: The `down_revision` variable is a string that holds the identifier of the previous database schema revision in a sequence of migrations managed by Alembic. It is used to establish a linear history of database schema changes, allowing Alembic to determine the order of migrations.
- **Use**: This variable is used by Alembic to identify the parent revision of the current migration, ensuring that migrations are applied in the correct order.


---
### branch\_labels
- **Type**: `NoneType`
- **Description**: The `branch_labels` variable is a global variable set to `None`. It is part of the Alembic migration script metadata, which typically includes information about the migration's relationship to other branches or labels in a version control system.
- **Use**: This variable is used to define branch labels for the migration script, but in this case, it is not utilized as it is set to `None`.


---
### depends\_on
- **Type**: `NoneType`
- **Description**: The variable `depends_on` is a global variable set to `None`. It is part of the Alembic migration script metadata, which typically indicates dependencies on other migrations.
- **Use**: This variable is used to specify that the current migration does not depend on any other migrations.


# Functions

---
### upgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2025_03_12_1110-47fbcf862323_node_depth.upgrade}} -->
The `upgrade` function adds a new computed column 'depth' to the 'v2_node' table and creates an index on this column using Alembic operations.
- **Inputs**: None
- **Control Flow**:
    - The function begins by adding a new column named 'depth' to the 'v2_node' table using the `op.add_column` method.
    - The 'depth' column is defined as an `Integer` and is computed based on the length of the 'relative_path' string, specifically calculating the number of directory levels by counting slashes.
    - The computed column is set to be persisted, meaning its value is stored in the database rather than recalculated on each query.
    - The function then creates an index on the 'depth' column using the `op.create_index` method to potentially improve query performance on this column.
- **Output**: The function does not return any value; it performs database schema modifications.


---
### downgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2025_03_12_1110-47fbcf862323_node_depth.downgrade}} -->
The `downgrade` function reverses database schema changes by removing the 'depth' column and its associated index from the 'v2_node' table.
- **Inputs**: None
- **Control Flow**:
    - The function begins by dropping the index 'ix_v2_node_depth' from the 'v2_node' table using the `op.drop_index` method.
    - It then removes the 'depth' column from the 'v2_node' table using the `op.drop_column` method.
- **Output**: The function does not return any output; it performs schema changes on the database.



---
Made with ❤️ by [Driver](https://www.driver.ai/)