<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `2024_06_24_2134-b133aee202aa_add_indexes.py` file in the `python-backend` codebase contains an Alembic migration script that adds and removes indexes on various columns in the `codebases`, `derived_contents`, and `source_contents` tables.

# Purpose
This source code file is an Alembic migration script designed to manage database schema changes by adding and removing indexes. It provides narrow functionality, specifically focusing on creating and dropping indexes on several columns across different tables, such as `codebases`, `derived_contents`, and `source_contents`. The [`upgrade`](#py upgrade) function defines the creation of these indexes to potentially improve query performance, while the [`downgrade`](#py downgrade) function reverses these changes by dropping the indexes, allowing for easy rollback of the migration. The script is part of a version-controlled database schema management process, identified by unique revision identifiers, and is auto-generated by Alembic, a database migration tool for SQLAlchemy.
# Imports and Dependencies

---
- `alembic.op`


# Global Variables

---
### revision
- **Type**: `string`
- **Description**: The `revision` variable is a string that represents the unique identifier for the current database migration script. It is used by Alembic, a database migration tool for SQLAlchemy, to track the version of the database schema that this script applies.
- **Use**: This variable is used by Alembic to identify the specific migration script when applying or rolling back database schema changes.


---
### down\_revision
- **Type**: `str`
- **Description**: The `down_revision` variable is a string that holds the identifier of the previous database schema revision in an Alembic migration script. It is used to establish a linear sequence of migrations, allowing Alembic to determine the order in which migrations should be applied.
- **Use**: This variable is used by Alembic to track and apply database schema changes in the correct order.


---
### branch\_labels
- **Type**: `NoneType`
- **Description**: The variable `branch_labels` is a global variable set to `None`. It is part of the Alembic migration script metadata, which can be used to label branches in a migration script for organizational purposes.
- **Use**: This variable is used to potentially label branches in the Alembic migration process, although it is currently not assigned any value.


---
### depends\_on
- **Type**: `NoneType`
- **Description**: The `depends_on` variable is a global variable set to `None`. It is part of the Alembic migration script metadata, which is used to specify dependencies between migration scripts.
- **Use**: This variable is used to indicate that the current migration script does not depend on any other migration scripts.


# Functions

---
### upgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_06_24_2134-b133aee202aa_add_indexes.py .upgrade}} -->
The `upgrade` function creates several non-unique indexes on specified columns of the 'codebases', 'derived_contents', and 'source_contents' tables using Alembic operations.
- **Inputs**: None
- **Control Flow**:
    - The function begins by creating an index on the 'workspace_id' column of the 'codebases' table.
    - It then creates an index on the 'source_content_id' column of the 'derived_contents' table.
    - Next, it creates an index on the 'derived_content_type_id' column of the 'derived_contents' table.
    - An index is created on the 'codebase_id' column of the 'source_contents' table.
    - Another index is created on the 'source_content_type_id' column of the 'source_contents' table.
    - Finally, an index is created on the 'workspace_id' column of the 'source_contents' table.
- **Output**: The function does not return any value; it performs database schema modifications by creating indexes.


---
### downgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_06_24_2134-b133aee202aa_add_indexes.py .downgrade}} -->
The `downgrade` function removes specific database indexes from the tables `source_contents`, `derived_contents`, and `codebases` as part of a database migration rollback.
- **Inputs**: None
- **Control Flow**:
    - The function begins by calling `op.drop_index` to remove the index `ix_source_contents_workspace_id` from the `source_contents` table.
    - It then removes the index `ix_source_contents_source_content_type_id` from the `source_contents` table.
    - Next, it removes the index `ix_source_contents_codebase_id` from the `source_contents` table.
    - The function proceeds to remove the index `ix_derived_contents_source_content_id` from the `derived_contents` table.
    - It also removes the index `ix_derived_contents_derived_content_type_id` from the `derived_contents` table.
    - Finally, it removes the index `ix_codebases_workspace_id` from the `codebases` table.
- **Output**: The function does not return any value; it performs operations to modify the database schema by removing indexes.



---
Made with ❤️ by [Driver](https://www.driver.ai/)