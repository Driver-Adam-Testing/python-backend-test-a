
## Files
- **[gitlab_provider.py](providers/gitlab_provider.py.driver.md)**: The `gitlab_provider.py` file implements the `GitLabProvider` class, which manages GitLab OAuth authorization, access token handling, repository fetching, and cloning operations, integrating with AWS for secret management and file storage.
