<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `2025_01_17_1039-d28421c6e47b_cleanup_models.py` file contains an Alembic migration script for the `python-backend` codebase that removes several indexes, constraints, columns, and tables, and also provides a downgrade function to reverse these changes.

# Purpose
This Python file is an Alembic migration script used to manage changes to a database schema. It is specifically designed to perform a cleanup operation on the database models by removing certain tables, columns, indexes, and foreign key constraints. The script defines two main functions: `upgrade()` and `downgrade()`. The `upgrade()` function executes a series of operations to drop indexes, constraints, columns, and entire tables, effectively cleaning up the database schema by removing obsolete or unnecessary components. It also creates a new foreign key constraint to maintain referential integrity after the cleanup. Conversely, the `downgrade()` function reverses these changes, restoring the dropped tables, columns, indexes, and constraints to their original state, allowing for a rollback of the schema changes if needed.

The script is part of a broader database migration framework, as indicated by its use of Alembic, a popular database migration tool for SQLAlchemy. It is not intended to be a standalone script but rather a component of a larger migration process, typically executed as part of a series of migrations to evolve the database schema over time. The script does not define public APIs or external interfaces; instead, it focuses on internal database structure modifications. The presence of revision identifiers and dependencies suggests that this migration is part of a sequence, ensuring that changes are applied in the correct order relative to other migrations.
# Imports and Dependencies

---
- `sqlalchemy`
- `alembic.op`
- `sqlalchemy.dialects.postgresql`


# Global Variables

---
### revision
- **Type**: `string`
- **Description**: The `revision` variable is a string that holds the unique identifier for the current database schema revision in an Alembic migration script. It is used to track the specific version of the database schema that this migration script represents.
- **Use**: This variable is used by Alembic to identify the current migration version when applying or rolling back database schema changes.


---
### down\_revision
- **Type**: `string`
- **Description**: The `down_revision` variable is a string that holds the identifier of the previous database schema revision in an Alembic migration script. It is used to establish a linear sequence of migrations by indicating which revision this migration is based on.
- **Use**: This variable is used by Alembic to determine the order of database migrations.


---
### branch\_labels
- **Type**: `NoneType`
- **Description**: The variable `branch_labels` is a global variable set to `None`. It is part of the Alembic migration script metadata, which typically includes information about the migration such as revision identifiers and dependencies.
- **Use**: This variable is used to define branch labels for the migration, but in this case, it is not utilized as it is set to `None`.


---
### depends\_on
- **Type**: `NoneType`
- **Description**: The `depends_on` variable is a global variable set to `None`. It is part of the Alembic migration script metadata, which typically indicates dependencies on other migrations.
- **Use**: This variable is used to specify if the current migration depends on any other migrations, but in this case, it indicates no dependencies by being set to `None`.


# Functions

---
### upgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2025_01_17_1039-d28421c6e47b_cleanup_models.upgrade}} -->
The `upgrade` function performs a series of database schema modifications, including dropping indexes, constraints, columns, and tables, and creating a new foreign key constraint.
- **Inputs**: None
- **Control Flow**:
    - Drop several indexes from the 'codebases' and 'derived_contents' tables.
    - Drop foreign key constraints from 'document_sources', 'inspectorrun', and 'codebases' tables.
    - Remove columns from 'runtimelogagentinstance', 'derived_contents', and 'document_sources' tables.
    - Drop several tables including 'workspaces', 'codebases', 'inspection_versions', 'tags_contents', and 'derived_content_types'.
    - Create a new index on the 'tags' table for the 'type' column.
    - Create a new foreign key constraint on the 'inspectorrun' table referencing the 'v2_version' table.
- **Output**: The function does not return any value; it modifies the database schema as specified.


---
### downgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2025_01_17_1039-d28421c6e47b_cleanup_models.downgrade}} -->
The `downgrade` function reverts database schema changes by recreating tables, columns, constraints, and indexes that were previously dropped.
- **Inputs**: None
- **Control Flow**:
    - Create the 'derived_content_types' table with columns 'id', 'type_name', 'created_at', and 'updated_at', and set primary and unique constraints.
    - Create the 'tags_contents' table with columns 'tag_id', 'content_id', and 'include', and set primary and foreign key constraints.
    - Create the 'inspection_versions' table with columns 'id', 'version', 'display_name', 'created_at', 'updated_at', and 'previous_version_id', and set a primary key constraint.
    - Create the 'codebases' table with columns 'id', 'workspace_id', 'codebase_name', 'description', 'status', 'storage_url', 'resource_root', 'creator_id', 'created_at', and 'updated_at', and set primary, unique, and foreign key constraints.
    - Create the 'workspaces' table with columns 'id', 'display_name', 'description', 'organization_id', 'created_at', and 'updated_at', and set a primary key constraint.
    - Create an index on the 'workspace_id' column of the 'codebases' table.
    - Drop and recreate an index on the 'type' column of the 'tags' table.
    - Add 'workspace_id' and 'codebase_id' columns to the 'runtimelogagentinstance' table.
    - Drop and recreate a foreign key constraint on the 'version_id' column of the 'inspectorrun' table.
    - Add 'source_id', 'include', and 'document_id' columns to the 'document_sources' table and create foreign key constraints for 'document_id' and 'source_id'.
    - Add 'workspace_id', 'status', and 'codebase_id' columns to the 'derived_contents' table and create indexes on 'workspace_id', 'source_content_id', 'content_type_id', 'content_name', and 'codebase_id'.
- **Output**: The function does not return any value; it performs database schema modifications.



---
Made with ❤️ by [Driver](https://www.driver.ai/)