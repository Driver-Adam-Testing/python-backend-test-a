<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `2024_07_24_0950-54472237f688_add_order_and_content_to_sc.py` file is an Alembic migration script that adds "order" and "content" columns to the "source_contents" table and modifies related indices in the `python-backend` codebase.

# Purpose
This Python script is an Alembic migration file used to modify a database schema, specifically for a table named `source_contents`. It provides narrow functionality focused on altering the structure of this table by adding two new columns, `order` and `content`, and adjusting the indices associated with the `content_type_id` column. The [`upgrade`](<#upgrade>) function implements these changes, including setting a default value for the `order` column, while the [`downgrade`](<#downgrade>) function attempts to reverse these changes, although it notes potential bugs. This script is part of a version-controlled database migration process, ensuring that schema changes are systematically applied and can be rolled back if necessary.
# Imports and Dependencies

---
- `alembic.op`
- `sqlalchemy`


# Global Variables

---
### revision
- **Type**: `string`
- **Description**: The `revision` variable is a string that represents the unique identifier for the current database migration script. It is used by Alembic, a database migration tool for SQLAlchemy, to track the version of the database schema that this script applies.
- **Use**: This variable is used by Alembic to identify and apply the specific migration when upgrading or downgrading the database schema.


---
### down\_revision
- **Type**: `str`
- **Description**: The `down_revision` variable is a string that holds the identifier of the previous database schema revision in an Alembic migration script. It is used to establish a linear sequence of migrations, allowing Alembic to determine the order in which migrations should be applied.
- **Use**: This variable is used by Alembic to track the migration history and ensure that migrations are applied in the correct order.


---
### branch\_labels
- **Type**: `NoneType`
- **Description**: The `branch_labels` variable is a global variable set to `None`. It is part of the Alembic migration script metadata, which typically includes information about the migration such as revision identifiers and dependencies.
- **Use**: This variable is used to define branch labels for the migration, but in this case, it is not utilized as it is set to `None`.


---
### depends\_on
- **Type**: `NoneType`
- **Description**: The `depends_on` variable is a global variable set to `None`. It is used in the context of Alembic migrations to specify dependencies between migration scripts.
- **Use**: This variable is used to indicate that the current migration script does not depend on any other migration script.


# Functions

---
### upgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_07_24_0950-54472237f688_add_order_and_content_to_sc.upgrade}} -->
The `upgrade` function modifies the database schema by adding new columns, updating indices, and setting default values for a table.
- **Inputs**: None
- **Control Flow**:
    - Add a new column 'order' of type Integer to the 'source_contents' table, allowing null values.
    - Add a new column 'content' of type Text to the 'source_contents' table, allowing null values.
    - Drop the existing index 'ix_source_contents_source_content_type_id' from the 'source_contents' table.
    - Create a new index on the 'content_type_id' column of the 'source_contents' table with a new name generated by the `op.f` function, ensuring it is not unique.
    - Alter the 'order' column in the 'source_contents' table to set a server default value of 0 for future inserts.
- **Output**: The function does not return any value; it performs schema modifications on the database.


---
### downgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_07_24_0950-54472237f688_add_order_and_content_to_sc.downgrade}} -->
The `downgrade` function reverses database schema changes by dropping and recreating an index and removing two columns from the `source_contents` table.
- **Inputs**: None
- **Control Flow**:
    - The function starts by dropping an index named `ix_source_contents_content_type_id` from the `source_contents` table using the `op.drop_index` method.
    - It then creates a new index named `ix_source_contents_source_content_type_id` on the `source_contents` table for the `content_type_id` column using the `op.create_index` method.
    - The function proceeds to drop the `content` column from the `source_contents` table using the `op.drop_column` method.
    - Finally, it drops the `order` column from the `source_contents` table using the `op.drop_column` method.
- **Output**: The function does not return any value; it performs schema changes on the database.



---
Made with ❤️ by [Driver](https://www.driver.ai/)