<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `2024_06_24_2134-b133aee202aa_add_indexes.py` file in the `python-backend` codebase contains an Alembic migration script that adds and removes indexes on various database tables to optimize query performance.

# Purpose
This Python script is an Alembic migration file used to manage database schema changes, specifically for adding and removing indexes on certain database tables. The script provides narrow functionality, focusing solely on the creation and deletion of indexes to optimize query performance on the "codebases," "derived_contents," and "source_contents" tables. The [`upgrade`](<#py upgrade>) function defines the creation of non-unique indexes on specific columns, while the [`downgrade`](<#py downgrade>) function reverses these changes by dropping the indexes. This file is part of a version-controlled database schema management system, where each migration is identified by a unique revision ID and can be applied or rolled back as needed.
# Imports and Dependencies

---
- `alembic.op`


# Global Variables

---
### revision
- **Type**: `string`
- **Description**: The `revision` variable is a string that represents the unique identifier for the current database migration script. It is used by Alembic, a database migration tool for SQLAlchemy, to track the version of the database schema that this script applies.
- **Use**: This variable is used by Alembic to identify and apply the specific migration script to the database.


---
### down\_revision
- **Type**: `str`
- **Description**: The `down_revision` variable is a string that holds the identifier of the previous database schema revision in an Alembic migration script. It is used to establish a linear sequence of migrations by indicating which revision this migration is based on.
- **Use**: This variable is used by Alembic to determine the order of database migrations.


---
### branch\_labels
- **Type**: `NoneType`
- **Description**: The variable `branch_labels` is a global variable set to `None`. It is part of the Alembic migration script metadata, which typically includes information about the migration such as revision identifiers and dependencies.
- **Use**: `branch_labels` is used to define labels for a branch in Alembic migrations, but in this script, it is not utilized and remains set to `None`.


---
### depends\_on
- **Type**: `NoneType`
- **Description**: The `depends_on` variable is a global variable set to `None`. It is part of the Alembic migration script metadata, which typically includes information about dependencies between migration scripts.
- **Use**: This variable is used to indicate that the current migration script does not depend on any other migration scripts.


# Functions

---
### upgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_06_24_2134-b133aee202aa_add_indexes.py .upgrade}} -->
The `upgrade` function creates several non-unique indexes on specified columns of different database tables using Alembic operations.
- **Inputs**: None
- **Control Flow**:
    - The function begins by creating an index on the 'workspace_id' column of the 'codebases' table.
    - It then creates an index on the 'source_content_id' column of the 'derived_contents' table.
    - Next, it creates an index on the 'derived_content_type_id' column of the 'derived_contents' table.
    - An index is created on the 'codebase_id' column of the 'source_contents' table.
    - The function creates another index on the 'source_content_type_id' column of the 'source_contents' table.
    - Finally, it creates an index on the 'workspace_id' column of the 'source_contents' table.
- **Output**: The function does not return any value; it performs database schema modifications by creating indexes.


---
### downgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_06_24_2134-b133aee202aa_add_indexes.py .downgrade}} -->
The `downgrade` function removes specific database indexes from the `source_contents`, `derived_contents`, and `codebases` tables as part of a database migration rollback.
- **Inputs**: None
- **Control Flow**:
    - The function begins by dropping the index `ix_source_contents_workspace_id` from the `source_contents` table.
    - It then drops the index `ix_source_contents_source_content_type_id` from the `source_contents` table.
    - Next, it drops the index `ix_source_contents_codebase_id` from the `source_contents` table.
    - The function proceeds to drop the index `ix_derived_contents_source_content_id` from the `derived_contents` table.
    - It also drops the index `ix_derived_contents_derived_content_type_id` from the `derived_contents` table.
    - Finally, it drops the index `ix_codebases_workspace_id` from the `codebases` table.
- **Output**: The function does not return any value; it performs operations on the database schema to remove specified indexes.



---
Made with ❤️ by [Driver](https://www.driver.ai/)