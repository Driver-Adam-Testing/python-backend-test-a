<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `2024_07_24_1018-3844d00aa769_add_rel_path_to_sc.py` file is an Alembic migration script that adds a non-nullable `relative_path` column to the `derived_contents` table, populates it with data from the `source_contents` table, and then removes the temporary default value.

# Purpose
This Python file is an Alembic migration script designed to modify a database schema by adding a new column to an existing table. Specifically, it targets the `derived_contents` table, introducing a new column named `relative_path` of type `Text`. The script is structured to ensure a smooth transition by initially adding the column with a temporary default value, then populating it with data from a related table, `source_contents`, using an SQL `UPDATE` statement. Finally, it removes the temporary default value to complete the migration process. This approach ensures that the database remains consistent and functional throughout the migration.

The script defines two primary functions: `upgrade()` and `downgrade()`. The `upgrade()` function implements the steps necessary to apply the migration, while the `downgrade()` function provides a mechanism to reverse the changes by removing the `relative_path` column. This file is part of a version-controlled database schema management system, where each migration is identified by a unique revision ID (`3844d00aa769`) and is linked to a previous migration (`54472237f688`). This script is intended to be executed as part of a larger migration process, ensuring that database schema changes are applied consistently and can be rolled back if necessary.
# Imports and Dependencies

---
- `alembic.op`
- `sqlalchemy`


# Global Variables

---
### revision
- **Type**: `string`
- **Description**: The `revision` variable is a string that represents the unique identifier for the current database migration script. It is used by Alembic, a database migration tool for SQLAlchemy, to track and apply changes to the database schema.
- **Use**: This variable is used by Alembic to identify the specific migration script when applying or rolling back database schema changes.


---
### down\_revision
- **Type**: `str`
- **Description**: The `down_revision` variable is a string that holds the identifier of the previous database schema revision in a sequence of migrations managed by Alembic. It is used to establish a linear history of database changes, allowing Alembic to determine the order of migrations.
- **Use**: This variable is used by Alembic to identify the immediate predecessor of the current migration, ensuring that migrations are applied in the correct sequence.


---
### branch\_labels
- **Type**: `NoneType`
- **Description**: The variable `branch_labels` is a global variable set to `None`. It is part of the Alembic migration script metadata, which typically includes information about the migration such as revision identifiers and dependencies.
- **Use**: This variable is used to define branch labels for the migration, but in this case, it is not utilized as it is set to `None`.


---
### depends\_on
- **Type**: `NoneType`
- **Description**: The variable `depends_on` is a global variable set to `None`. It is part of the Alembic migration script metadata, which is used to specify dependencies between migration scripts.
- **Use**: This variable is used to indicate that the current migration script does not depend on any other migration scripts.


# Functions

---
### upgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_07_24_1018-3844d00aa769_add_rel_path_to_sc.upgrade}} -->
The `upgrade` function adds a new column `relative_path` to the `derived_contents` table, populates it with data from the `source_contents` table, and then removes the temporary default value.
- **Inputs**: None
- **Control Flow**:
    - Add a new column `relative_path` to the `derived_contents` table with a temporary default value of an empty string.
    - Execute an SQL update statement to populate the `relative_path` column with values from the `source_contents` table based on a matching `source_content_id`.
    - Remove the temporary default value from the `relative_path` column.
- **Output**: The function does not return any value; it performs database schema and data modifications.


---
### downgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_07_24_1018-3844d00aa769_add_rel_path_to_sc.downgrade}} -->
The `downgrade` function removes the 'relative_path' column from the 'derived_contents' table.
- **Inputs**: None
- **Control Flow**:
    - The function calls `op.drop_column` to remove the 'relative_path' column from the 'derived_contents' table.
- **Output**: The function does not return any value.



---
Made with ❤️ by [Driver](https://www.driver.ai/)