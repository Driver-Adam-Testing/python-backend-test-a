<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `2024_12_23_1428-04593913d59f_make_content_kind_non_nullable.py` file contains an Alembic migration script that alters the `content_kind` column in the `derived_contents` table to be non-nullable and creates an index on it.

# Purpose
This source code file is a database migration script generated by Alembic, a database migration tool for SQLAlchemy. It provides narrow functionality, specifically altering the schema of a database table named `derived_contents`. The script's purpose is to make the `content_kind` column non-nullable by changing its type from `VARCHAR` to `Text` and creating an index on this column to potentially improve query performance. The [`upgrade`](#upgrade) function implements these changes, while the [`downgrade`](#downgrade) function reverses them, ensuring that the migration can be rolled back if necessary. This script is part of a version-controlled sequence of migrations, as indicated by the revision identifiers.
# Imports and Dependencies

---
- `sqlalchemy`
- `alembic.op`


# Global Variables

---
### revision
- **Type**: `string`
- **Description**: The `revision` variable is a string that holds the unique identifier for the current database migration script. It is used by Alembic, a database migration tool for SQLAlchemy, to track the specific version of the database schema that this script represents.
- **Use**: This variable is used by Alembic to identify and apply the correct migration when upgrading or downgrading the database schema.


---
### down\_revision
- **Type**: `string`
- **Description**: The `down_revision` variable is a string that holds the identifier of the previous database schema revision in a sequence of migrations managed by Alembic. It is used to establish a linear history of database changes, allowing Alembic to determine the order of migrations.
- **Use**: This variable is used by Alembic to identify the parent revision of the current migration, ensuring the correct application of database schema changes.


---
### branch\_labels
- **Type**: `NoneType`
- **Description**: The variable `branch_labels` is a global variable set to `None`. It is part of the Alembic migration script metadata, which typically includes information about the migration such as revision identifiers and dependencies.
- **Use**: This variable is used to define branch labels for the migration script, but in this case, it is not utilized as it is set to `None`.


---
### depends\_on
- **Type**: `NoneType`
- **Description**: The `depends_on` variable is a global variable set to `None`. It is used in the context of Alembic migrations to specify dependencies between migration scripts.
- **Use**: This variable is used to indicate that the current migration script does not depend on any other migration script.


# Functions

---
### upgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_12_23_1428-04593913d59f_make_content_kind_non_nullable.upgrade}} -->
The `upgrade` function modifies the database schema by altering the `content_kind` column type and creating an index on it in the `derived_contents` table.
- **Inputs**: None
- **Control Flow**:
    - The function begins by altering the `content_kind` column in the `derived_contents` table, changing its type from `VARCHAR` to `Text` while keeping it nullable.
    - It then creates a non-unique index on the `content_kind` column in the `derived_contents` table using Alembic's `op.create_index` function.
- **Output**: The function does not return any value; it performs schema modifications on the database.


---
### downgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_12_23_1428-04593913d59f_make_content_kind_non_nullable.downgrade}} -->
The `downgrade` function reverses database schema changes by dropping an index and altering a column type in the `derived_contents` table.
- **Inputs**: None
- **Control Flow**:
    - The function begins by dropping the index `ix_derived_contents_content_kind` from the `derived_contents` table using `op.drop_index`.
    - It then alters the `content_kind` column in the `derived_contents` table, changing its type from `Text` to `VARCHAR` using `op.alter_column`.
- **Output**: The function does not return any value; it performs schema changes on the database.



---
Made with ❤️ by [Driver](https://www.driver.ai/)