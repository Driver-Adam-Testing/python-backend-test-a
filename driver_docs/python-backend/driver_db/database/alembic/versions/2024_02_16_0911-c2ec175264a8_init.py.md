<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `2024_02_16_0911-c2ec175264a8_init.py` file is an Alembic migration script that creates tables and indexes for content processing sessions, content metadata, and chunks, while also ensuring necessary PostgreSQL extensions are available.

# Purpose
This Python file is an Alembic migration script used to manage changes to a database schema. It defines both an [`upgrade`](#upgrade) function and a [`downgrade`](#downgrade) function, which are standard in Alembic migrations to apply and revert schema changes, respectively. The script creates three tables: `contentprocessingsession`, `contentmetadata`, and `chunk`. These tables are designed to store information about content processing sessions, metadata associated with content, and chunks of text, respectively. The `contentmetadata` table includes a foreign key relationship to the `contentprocessingsession` table, and the `chunk` table includes a foreign key relationship to the `contentmetadata` table, indicating a structured relationship between these entities.

The script also includes commands to create PostgreSQL extensions if they do not already exist, specifically the `vector` and `uuid-ossp` extensions, which are used for vector operations and UUID generation, respectively. Additionally, the script creates several indexes on the `contentmetadata` table to optimize queries based on `codebase_id`, `relative_path`, and `workspace_id`. The use of the `pgvector` library for vector operations suggests that the application may involve machine learning or data processing tasks that require vector representations. Overall, this migration script is a crucial component of a database management system, ensuring that the database schema evolves in a controlled and reversible manner.
# Imports and Dependencies

---
- `collections.abc.Sequence`
- `pgvector`
- `sqlalchemy`
- `sqlmodel`
- `alembic.op`


# Global Variables

---
### revision
- **Type**: `str`
- **Description**: The `revision` variable is a string that represents the unique identifier for the current database schema revision in an Alembic migration script. It is used to track the specific version of the database schema that this migration script applies to.
- **Use**: This variable is used by Alembic to identify and apply the correct database schema changes during the migration process.


---
### down\_revision
- **Type**: `str`
- **Description**: The `down_revision` variable is a string that holds the identifier of the previous database schema revision in an Alembic migration script. It is used to establish a linear sequence of database schema changes, allowing Alembic to track and apply migrations in the correct order.
- **Use**: This variable is used by Alembic to determine the predecessor of the current migration, ensuring that migrations are applied in the correct sequence.


---
### branch\_labels
- **Type**: `str | Sequence[str] | None`
- **Description**: The `branch_labels` variable is a global variable that can hold a string, a sequence of strings, or be set to None. It is used in the context of Alembic, a database migration tool for SQLAlchemy, to potentially label a branch in a database migration script.
- **Use**: This variable is used to specify labels for a branch in a database migration, allowing for more organized and identifiable migration paths.


---
### depends\_on
- **Type**: `str | Sequence[str] | None`
- **Description**: The `depends_on` variable is a global variable that can hold a string, a sequence of strings, or be set to None. It is used in the context of Alembic migrations to specify dependencies between different database schema revisions.
- **Use**: This variable is used to define which other revisions the current revision depends on, helping to manage the order of migrations.


# Functions

---
### upgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_02_16_0911-c2ec175264a8_init.upgrade}} -->
The `upgrade` function applies database schema changes using Alembic to create tables and indexes necessary for content processing and metadata management.
- **Inputs**: None
- **Control Flow**:
    - Execute SQL commands to create PostgreSQL extensions 'vector' and 'uuid-ossp' if they do not exist.
    - Create a table named 'contentprocessingsession' with columns for 'id', 'code_revision', and 'arguments', and set 'id' as the primary key.
    - Create a table named 'contentmetadata' with columns for 'id', 'workspace_id', 'codebase_id', 'content_type', 'relative_path', 'misc_metadata', and 'content_processing_session_id', with 'id' as the primary key and a foreign key constraint on 'content_processing_session_id'.
    - Create indexes on 'contentmetadata' for 'codebase_id', 'relative_path', and 'workspace_id'.
    - Create a table named 'chunk' with columns for 'id', 'content_metadata_id', 'text', 'text_embedding_3_small', and 'chunk_number', with 'id' as the primary key and a foreign key constraint on 'content_metadata_id'.
- **Output**: The function does not return any value; it performs database schema modifications.


---
### downgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_02_16_0911-c2ec175264a8_init.downgrade}} -->
The `downgrade` function reverses database schema changes by dropping specific tables and indexes.
- **Inputs**: None
- **Control Flow**:
    - The function begins by dropping the 'chunk' table using `op.drop_table`.
    - It then drops the index on 'workspace_id' in the 'contentmetadata' table using `op.drop_index`.
    - Next, it drops the index on 'relative_path' in the 'contentmetadata' table.
    - It proceeds to drop the index on 'codebase_id' in the 'contentmetadata' table.
    - The function then drops the 'contentmetadata' table.
    - Finally, it drops the 'contentprocessingsession' table.
- **Output**: The function does not return any value; it performs schema changes directly on the database.



---
Made with ❤️ by [Driver](https://www.driver.ai/)