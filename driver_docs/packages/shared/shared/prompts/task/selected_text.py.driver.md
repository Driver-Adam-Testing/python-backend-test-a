# Purpose
This code defines two global variables, `PROMPT` and `MESSAGE`, which are likely used in a broader context involving text processing or a chat system. The `PROMPT` variable contains a string that instructs a system to generate markdown-compatible text that seamlessly integrates with surrounding content. The `MESSAGE` variable is a dictionary with two keys: `role`, which is set to "system", and `content`, which holds the `PROMPT` string. This setup suggests that the code is part of a configuration or initialization script for a system that processes or generates text, providing narrow functionality focused on defining specific message parameters.
# Global Variables

---
### MESSAGE 
- **Type**: `dict`
- **Description**: The variable `MESSAGE` is a dictionary with two key-value pairs. The first key is 'role' with the value 'system', and the second key is 'content' with the value of the string stored in the `PROMPT` variable. This dictionary is likely used to define a message structure for a system role in a communication or messaging context.
- **Use**: This variable is used to store a structured message format that includes a role and content, which can be utilized in a system that processes or sends messages.


---
### PROMPT 
- **Type**: `str`
- **Description**: The variable `PROMPT` is a string that contains a specific instruction for generating text. It specifies that the response should be replacement or appendable text, formatted as markdown, and should be cohesive and readable within the surrounding context.
- **Use**: This variable is used to define the content of a system message, likely for a text generation or processing task.


