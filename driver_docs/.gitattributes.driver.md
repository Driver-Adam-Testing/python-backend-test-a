# Purpose
This file is a `.gitattributes` configuration file used in a Git repository. It specifies how Git should handle line endings for different file types. The `* text=auto` line ensures that Git automatically normalizes line endings for all files, while the `*.sh text eol=lf` line enforces the use of LF (Line Feed) line endings specifically for shell script files with the `.sh` extension.
