<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `2024_07_24_0950-54472237f688_add_order_and_content_to_sc.py` file is an Alembic migration script that adds "order" and "content" columns to the "source_contents" table and modifies related indices in the `python-backend` codebase.

# Purpose
This source code file is an Alembic migration script used to modify a database schema, specifically for a table named `source_contents`. It provides narrow functionality, focusing on adding two new columns, `order` and `content`, to the table and adjusting the indices related to the `content_type_id` column. The [`upgrade`](#upgrade) function implements these changes, including setting a server default for the `order` column, while the [`downgrade`](#downgrade) function attempts to reverse these changes, although it notes potential bugs. This script is part of a version-controlled database migration process, ensuring that the database schema can be updated or reverted in a controlled manner.
# Imports and Dependencies

---
- `alembic.op`
- `sqlalchemy`


# Global Variables

---
### revision
- **Type**: `string`
- **Description**: The `revision` variable is a string that represents the unique identifier for the current database schema migration. It is used by Alembic, a database migration tool for SQLAlchemy, to track changes to the database schema over time.
- **Use**: This variable is used to identify the specific migration script in the Alembic migration history.


---
### down\_revision
- **Type**: `str`
- **Description**: The `down_revision` variable is a string that holds the identifier of the previous database schema revision in an Alembic migration script. It is used to establish a linear sequence of migrations, allowing Alembic to determine the order in which migrations should be applied.
- **Use**: This variable is used by Alembic to track and apply database schema changes in the correct order.


---
### branch\_labels
- **Type**: `NoneType`
- **Description**: The `branch_labels` variable is a global variable set to `None`. It is part of the Alembic migration script metadata, which is used to define characteristics of the migration, such as revision identifiers and dependencies.
- **Use**: This variable is used to specify branch labels for the migration, but in this case, it is not utilized as it is set to `None`.


---
### depends\_on
- **Type**: `NoneType`
- **Description**: The `depends_on` variable is a global variable set to `None`. It is used in the context of Alembic migrations to specify dependencies between migration scripts, indicating that this migration does not depend on any other migration.
- **Use**: This variable is used to define the dependency relationship of the current migration script with others, specifically indicating no dependencies.


# Functions

---
### upgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_07_24_0950-54472237f688_add_order_and_content_to_sc.upgrade}} -->
The `upgrade` function modifies the database schema by adding new columns, updating indices, and setting default values for a table named `source_contents`.
- **Inputs**: None
- **Control Flow**:
    - Adds a new column named 'order' of type Integer to the 'source_contents' table, allowing null values.
    - Adds a new column named 'content' of type Text to the 'source_contents' table, allowing null values.
    - Drops an existing index 'ix_source_contents_source_content_type_id' from the 'source_contents' table.
    - Creates a new index on the 'content_type_id' column of the 'source_contents' table with the name generated by `op.f` function, ensuring it is not unique.
    - Alters the 'order' column to set a server default value of 0 for future inserts, maintaining its Integer type.
- **Output**: The function does not return any value; it performs schema modifications on the database.


---
### downgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_07_24_0950-54472237f688_add_order_and_content_to_sc.downgrade}} -->
The `downgrade` function reverses database schema changes by dropping specific columns and modifying indices in the `source_contents` table.
- **Inputs**: None
- **Control Flow**:
    - The function starts by dropping an index named `ix_source_contents_content_type_id` from the `source_contents` table.
    - It then creates a new index named `ix_source_contents_source_content_type_id` on the `source_contents` table for the `content_type_id` column, ensuring it is not unique.
    - The function proceeds to drop the `content` column from the `source_contents` table.
    - Finally, it drops the `order` column from the `source_contents` table.
- **Output**: The function does not return any value; it performs schema modifications on the database.



---
Made with ❤️ by [Driver](https://www.driver.ai/)