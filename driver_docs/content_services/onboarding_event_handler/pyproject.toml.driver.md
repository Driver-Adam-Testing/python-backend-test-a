# Purpose
The provided file is a configuration file for the Python dependency management tool, Poetry. It defines the metadata and dependencies for a project named "onboarding-event-handler" with version "0.1.0". The file specifies the project's author, Eric Miller, and indicates that the README file is "README.md". It includes the source package located in "src/utils" and lists the required dependencies for the project, such as Python 3.12, boto3, httpx, and others, along with their respective version constraints. Additionally, it defines development dependencies under a separate group, including pytest and moto, and specifies build system requirements using "poetry-core". The file also includes configuration options for pytest, setting the test paths to "tests" and the Python path to "src".
