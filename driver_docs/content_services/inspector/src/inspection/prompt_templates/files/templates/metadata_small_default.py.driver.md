# Purpose
This code is a configuration setup for defining a template used in a system that likely deals with language processing or generation. It imports specific metadata prompts from a module named `utils.lang_specialization.metadata` and a template utility from `utils.templates`. The `METADATA_SMALL_TEMPLATE` is a list containing a tuple that organizes these imported prompts and a string identifier `S.SINGLE_PROMPT_TEXT` under a section labeled "# Purpose". This setup suggests a narrow functionality, primarily focused on structuring or formatting metadata prompts for a specific small-scale purpose, possibly for generating or displaying information in a consistent manner within a larger application.
# Imports and Dependencies

---
- `utils.lang_specialization.metadata`
- `utils.templates`


# Global Variables

---
### METADATA_SMALL_TEMPLATE 
- **Type**: `list`
- **Description**: `METADATA_SMALL_TEMPLATE` is a list containing a single tuple. This tuple consists of four elements: a constant `S.SINGLE_PROMPT_TEXT`, a string `"# Purpose"`, and two imported constants `METADATA_SMALL_SYSTEM_PROMPT` and `PURPOSE_PROMPT_SMALL`. The list is likely used to define a template structure for metadata with specific prompts and purposes.
- **Use**: This variable is used to store a template configuration for metadata, which includes specific prompts and purpose-related information.


