<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `2024_03_28_1619-77b384e95c97_add_runtime_logs.py` file contains an Alembic migration script that adds tables for runtime logging and removes the `contentprocessingsession` table in the `python-backend` codebase.

# Purpose
This Python file is an Alembic migration script designed to modify the database schema by adding new tables and removing existing ones. The primary purpose of this script is to introduce runtime logging capabilities by creating four new tables: `runtimelogagentinstance`, `runtimelogagenterror`, `runtimelogagentmessage`, and `runtimelogcontentretrieval`. These tables are structured to store information about agent instances, errors, messages, and content retrieval activities, respectively. Each table includes columns for timestamps, unique identifiers, and relevant foreign key constraints to maintain relationships between the tables. The script also removes the `contentprocessingsession` table and its associated foreign key from the `contentmetadata` table, indicating a shift in how content processing sessions are managed.

The script is structured with two main functions: `upgrade()` and `downgrade()`. The `upgrade()` function implements the changes to the database schema by creating the new tables and dropping the old ones, while the `downgrade()` function reverses these changes, restoring the previous schema state. This migration script is part of a broader database version control system, allowing developers to apply and revert schema changes systematically. The use of Alembic, SQLAlchemy, and SQLModel indicates that this script is part of a Python-based application that relies on these libraries for database management and migrations.
# Imports and Dependencies

---
- `collections.abc.Sequence`
- `sqlalchemy`
- `sqlmodel`
- `alembic.op`
- `sqlalchemy.dialects.postgresql`


# Global Variables

---
### revision
- **Type**: `str`
- **Description**: The `revision` variable is a string that represents the unique identifier for the current database migration script. It is used by Alembic, a database migration tool for SQLAlchemy, to track and apply changes to the database schema.
- **Use**: This variable is used by Alembic to identify the specific migration script being applied or rolled back.


---
### down\_revision
- **Type**: `str | None`
- **Description**: The `down_revision` variable is a string or None that represents the identifier of the previous database schema revision in an Alembic migration script. It is used to establish a linear sequence of migrations by indicating which revision this migration is based on.
- **Use**: This variable is used by Alembic to determine the order of migrations and ensure that they are applied in the correct sequence.


---
### branch\_labels
- **Type**: `str | Sequence[str] | None`
- **Description**: The `branch_labels` variable is a global variable that can hold a string, a sequence of strings, or be set to None. It is used in the context of Alembic, a database migration tool for SQLAlchemy, to potentially label branches in a version control system for database schema migrations.
- **Use**: This variable is used to specify branch labels for Alembic migrations, allowing for more complex migration paths and version control.


---
### depends\_on
- **Type**: `str | Sequence[str] | None`
- **Description**: The `depends_on` variable is a global variable that can hold a string, a sequence of strings, or be set to None. It is used to specify dependencies for a particular database migration script in the context of Alembic, a database migration tool for SQLAlchemy.
- **Use**: This variable is used to define dependencies for the Alembic migration script, indicating which other migrations must be applied before this one.


# Functions

---
### upgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_03_28_1619-77b384e95c97_add_runtime_logs.upgrade}} -->
The `upgrade` function applies database schema changes to add runtime log tables and remove obsolete tables and constraints.
- **Inputs**: None
- **Control Flow**:
    - Create a new table `runtimelogagentinstance` with columns for timestamps, UUIDs, and other identifiers, setting `id` as the primary key.
    - Create a new table `runtimelogagenterror` with columns for timestamps, UUIDs, and error messages, setting `id` as the primary key and establishing a foreign key relationship with `runtimelogagentinstance`.
    - Create a new table `runtimelogagentmessage` with columns for timestamps, UUIDs, JSON messages, and an order integer, setting `id` as the primary key and establishing a foreign key relationship with `runtimelogagentinstance`.
    - Create a new table `runtimelogcontentretrieval` with columns for timestamps, UUIDs, and foreign keys to `runtimelogagentinstance` and `chunk`, setting `id` as the primary key.
    - Drop the foreign key constraint `contentmetadata_content_processing_session_id_fkey` from the `contentmetadata` table.
    - Remove the `content_processing_session_id` column from the `contentmetadata` table.
    - Drop the `contentprocessingsession` table.
- **Output**: The function does not return any value as it is designed to perform database schema modifications.


---
### downgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_03_28_1619-77b384e95c97_add_runtime_logs.downgrade}} -->
The `downgrade` function reverses database schema changes by adding a column, creating a table, and dropping several other tables.
- **Inputs**: None
- **Control Flow**:
    - Add a column named 'content_processing_session_id' to the 'contentmetadata' table, allowing null values and using UUID type.
    - Create a new table named 'contentprocessingsession' with columns 'id', 'code_revision', 'arguments', 'created_at', and 'updated_at', setting 'id' as the primary key.
    - Establish a foreign key relationship between 'contentmetadata' and 'contentprocessingsession' using 'content_processing_session_id' and 'id'.
    - Drop the tables 'runtimelogcontentretrieval', 'runtimelogagentmessage', 'runtimelogagenterror', and 'runtimelogagentinstance'.
- **Output**: The function does not return any value; it performs schema modifications on the database.



---
Made with ❤️ by [Driver](https://www.driver.ai/)