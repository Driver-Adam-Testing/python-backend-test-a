<!--------------------------------------------------------------------------------->
<!-- IMPORTANT: This file is auto-generated by Driver (https://driver.ai). -------->
<!-- Manual edits may be overwritten on future commits. --------------------------->
<!--------------------------------------------------------------------------------->

The `2024_01_01_0000-8f59b93a989f_add_api_user.py` file is an Alembic migration script that adds a new API user to the database with specific privileges, using environment variables for the username and password.

# Purpose
This Python file is an Alembic migration script designed to add a new API user to a PostgreSQL database. The script is part of a database migration process, which is a common task in managing database schema changes over time. The primary function of this script is to create a new database user with specific privileges if the user does not already exist. It retrieves the username and password for the new user from environment variables (`NEW_API_USERNAME` and `NEW_API_PASSWORD`) and checks if these variables are set before proceeding. If the user already exists, it logs a notice; otherwise, it creates the user and grants them various privileges on the specified database and its public schema, including the ability to connect to the database and perform CRUD operations on all tables.

The script is structured to be executed as part of a migration process, with the [`upgrade`](<#upgrade>) function implementing the changes and the [`downgrade`](<#downgrade>) function left empty, indicating that the migration is not reversible through this script. This is typical for migrations that involve creating users or altering permissions, as reversing such changes might require additional logic not covered here. The use of Alembic's `op.execute` function allows the script to execute raw SQL commands, which are necessary for the detailed privilege management required. This script is a focused utility within a broader database management context, specifically targeting the setup of user access and permissions in a PostgreSQL environment.
# Imports and Dependencies

---
- `os`
- `alembic.op`


# Global Variables

---
### revision
- **Type**: `string`
- **Description**: The `revision` variable is a string that uniquely identifies the current database migration script. It is used by Alembic, a database migration tool for SQLAlchemy, to track the version of the database schema that this script represents.
- **Use**: This variable is used by Alembic to apply or rollback migrations in the correct order.


---
### down\_revision
- **Type**: `NoneType`
- **Description**: The `down_revision` variable is a global variable used in Alembic migration scripts to specify the identifier of the previous revision in the migration chain. In this script, it is set to `None`, indicating that this migration does not have a direct predecessor, possibly because it is the first migration or a standalone one.
- **Use**: `down_revision` is used by Alembic to determine the order of migrations and dependencies between them.


---
### branch\_labels
- **Type**: `NoneType`
- **Description**: The variable `branch_labels` is a global variable set to `None`. It is part of the Alembic migration script metadata, which typically includes identifiers for the migration such as `revision`, `down_revision`, and `branch_labels`. In this context, `branch_labels` is not being used to label any branches in the migration history.
- **Use**: `branch_labels` is used as part of the Alembic migration script metadata to potentially label branches, but in this script, it is set to `None` and not actively used.


---
### depends\_on
- **Type**: `NoneType`
- **Description**: The `depends_on` variable is a global variable set to `None`. It is used in the context of Alembic migrations to specify dependencies between migration scripts. In this case, it indicates that this migration does not depend on any other migration.
- **Use**: This variable is used to define migration dependencies in Alembic, but here it signifies no dependencies.


# Functions

---
### upgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_01_01_0000-8f59b93a989f_add_api_user.upgrade}} -->
The `upgrade` function creates a new database user with specific privileges if the required environment variables are set and the user does not already exist.
- **Inputs**: None
- **Control Flow**:
    - Retrieve the 'NEW_API_USERNAME', 'NEW_API_PASSWORD', and 'POSTGRES_DB' from environment variables.
    - Check if 'NEW_API_USERNAME' or 'NEW_API_PASSWORD' are not set; if so, print a message and exit the function.
    - Execute a SQL block to check if the user already exists in the database.
    - If the user does not exist, create the user with the specified password and grant various database privileges.
    - If the user already exists, raise a notice indicating the user already exists.
- **Output**: The function does not return any value; it performs database operations based on the presence of environment variables.


---
### downgrade<!-- {{#callable:python-backend/driver_db/database/alembic/versions/2024_01_01_0000-8f59b93a989f_add_api_user.downgrade}} -->
The `downgrade` function is a placeholder for reversing database schema changes, but currently does nothing.
- **Inputs**: None
- **Control Flow**:
    - The function is defined but contains no logic or operations, indicated by the `pass` statement.
- **Output**: The function does not produce any output or perform any operations.



---
Made with ❤️ by [Driver](https://www.driver.ai/)